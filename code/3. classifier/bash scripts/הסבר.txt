aaa_python_run_model_live [x]- מריץ את מודל מספר x (0-5) עם פלט למסך וגם לקובץ בתיקייה output עם תאריך ושעת הרצה.
לא רץ אם מתנתקים מהשרת. בנוסף מבטל הרצות python קודמות (הורג תהליך).

aaa_python_run_model [x]- כמו live רק שהריצה ברקע, כך שהוא רץ גם בעת התנתקות מהשרת ולא מציג פלט למסך (רק לקובץ).

current_state - מציג את הפלט של הריצה האחרונה. נרצה להריץ אותו גם בזמן הרצת aaa_python_run_model  (לא live) כדי לראות בקלות את הפלט של הריצה כרגע.

.bashrc - להוסיף את השורה שיש שם לסוף ה .bashrc בשרת על מנת שהשרת ישלח הודעת וואטסאפ ברגע שהשרת דלוק ללא הרצה.